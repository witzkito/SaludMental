{% extends "SaludMentalBundle::layout.html.twig" %}

{% block title %} Editar Persona{% endblock %}

{% block body %}
    <div class="container">
        <div class="col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-2 main">
                <form class="form" action="{{ path('edit_persona', {'id' : entity.id}) }}" method="post">
                        {{ form_rest(form) }}
                    <input type="submit" class="btn btn-default btn-ok" value="Editar">
                </form>
            
        </div>
    </div>
{% endblock %}
